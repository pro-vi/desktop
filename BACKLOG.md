# Backlog

Deferred work with reopen triggers. Each entry names the plan it came from and the observation that should reopen it — a deferral is a decision with a tripwire, not a drop.

## Deferred

- **Reusable consent-gated live-probe runner** (`scripts/live-probe.mjs`) — origin: plan `2026-09-18-001` (CI gate, probe convention, clean-environment acceptance). One driver for live provider probes (consent gate, respawn, exercise, verify, record, close tab) instead of a hand-rolled driver per probe; four were hand-rolled in the 2026-09-17 session alone. Reopen trigger: the next hand-rolled probe driver prompts a tally of the recurring mechanics (consent, lifecycle, timeout, cleanup, receipt) — reopen when estimated recurring savings exceed the runner's build + maintenance cost, not only when one future driver exceeds the whole build. Tally 2026-09-23 (the trigger fired): five hand-rolled probe sends in `docs/probes/2026-09-23-prompt-line-delivery-probe.md` each repeated the same nine steps — consent question, `/status` in-flight check, `agentify_shutdown` respawn, a byte-exact body built with `jq`, `POST /query` with `fireAndForget`, a `/runs/get` poll loop, a wait for `outputManifest.transcript.state == ready`, a Python diff of the snapshot's user turn against the prompt, and `POST /tabs/close` — plus a separate Electron frame-rate script. Reopen decision is the maintainer's.
- **Independent missing-run watchdog** — origin: plan `2026-09-18-001`. A scheduled workflow alerting when a pushed SHA has no CI run, independent of `ci.yml` itself. Reopen trigger: a second CI host exists (`nas/ROADMAP.md` Phase 6, the Mac Mini) — until then every runner shares this laptop, so an in-band watchdog is blind to exactly the failure it would guard.
- **GUI CI lane for the visual-proof tests** — origin: plan `2026-09-18-001` (surfaced when the first Linux run went red). The two `transcript-library-visual-proof` tests launch a real Electron renderer and skip on display-less Linux CI; their venue is local macOS runs. Reopen trigger: a visual-proof change whose evidence needs CI (or a regression that escaped local runs) — then either an xvfb lane on `ci-linux` (apt: `xvfb` + electron's shared libs) or a per-repo runner on `ci-macos` (the family precedent: fract-ai's desktop E2E jobs).
- **Isolate what makes ChatGPT drop a prompt line** — origin: inbox note `2026-09-18-prompt-line-with-angle-bracket-placeholders-dropped.md`; evidence in `docs/probes/2026-09-23-prompt-line-delivery-probe.md`. Four verbatim H1 bullets lose their third bullet on every send (4 of 4). `1e55f49` now reports the loss (`promptDelivery`, `prompt_delivery_incomplete`, and since 2026-09-25 the run label and a bounded `missingLines` list) but cannot prevent it. Angle brackets alone, the bare `<content>` in the preceding bullet, and a list directly after a paragraph are ruled out. A real (non-probe) loss recorded 2026-09-23 (run `df981474`, note `2026-09-23-incomplete-prompt-delivery-still-reports-success.md`): 8 of 136 lines lost, enumerated 2026-09-25 by re-reading the recorded user turn — length ruled out (an 80-char line lost while a 697-char line survived), no shared syntax (list items, bold-led paragraphs, a plain sentence); the one signal is position, all 8 in prompt lines 100–195 (the back half). Untested: the line ending in a closing backtick then a period (it survived whenever a label followed), and the full-length neighbor bullets. Next sends, each consented: the excerpt with a label after the final `` `. ``; without the consent bullet; with the taste bullet cut to one sentence. Reopen trigger: a real (non-probe) run records `promptDelivery.complete: false`, or a caller asks what text to avoid.
- **Quit when idle (fallback for idle cost)** — origin: ADR 0013. An app with no run for N minutes exits; the next MCP call respawns it (`ensureDesktopRunning`). Not taken on 2026-09-23 because every call after a quiet period would pay a cold start; `726df2e` closes idle tabs instead, which leaves the default tab rendering. Reopen trigger: macOS's power log (`/private/var/db/powerlog/Library/BatteryLife/CurrentPowerlog.PLSQL`, process table) shows Agentify's `Electron Helper (GPU)` as a top process again while few tabs are open.
- **Migrate the conversation reader to the data-turn-key DOM** — origin: inbox note `2026-09-25-pro-answer-unreadable-after-send.md`; the reopen trigger of the 2026-09-23 drift lane fired 2026-09-25 (runs `78d14939` and `a552e22d` both "Response reconciliation timed out"). ChatGPT's conversation page no longer carries `[data-message-author-role]`, `[data-message-id]`, or `[data-testid^="conversation-turn-"]` — live fingerprint (46K-char unblocked page, all legacy candidate selectors matching 0): turns live in `div[data-turn-key]` (UUID value, e.g. `304dcbf1-…`) under `div[data-content-search-turn-key]`, role labels are `h4` text ("You said:"), and the whole thread currently renders inside one `data-turn-key` container, so the per-turn mapping needs redesign (role from labels, provider id from turn-key values, ordinal from DOM order), not a selector swap. Broken until this lands: conversation reads, transcript sync, wait-loop turn qualification (Pro consults time out at the hard deadline with `pageTextChanged: true`), and `#readPromptDelivery`. `1bcfd4e` ships `captureDiagnostics` on every empty capture, which is how the fingerprint was taken and how the next session verifies the migration. Reopen trigger: none needed — this is live breakage; the first read after the migration should return `complete: true` on conversation `6ab48043` (and the sender's key `wezterm-attention-install-friction` should reconcile again).
- **ChatGPT compatibility drift seen 2026-09-23, not investigated** — origin: this session's `agentify_status` reads. Observations on 2026-09-23 (PDT): `submit` `anchor-postcondition-failed` 06:18; `response` `anchor-postcondition-failed` 06:23 and 16:25 (the 16:25 one recorded as run `29090c00` finished, which succeeded); `submit` `terminal-failed` 07:06, when run `dota-market-af0db3a8-harden3-call1` failed with "The provider UI never acknowledged the send action."; `readiness` `anchor-postcondition-failed` 17:44. The same `submit` and `response` failures occurred at 06:18 and 06:23, before any of that day's changes were loaded; whether the later ones share a cause is not checked. Superseded 2026-09-25: the trigger fired and the transcript half of the drift is the `data-turn-key` migration above; the submit/readiness observations remain unexplained.

## Measured candidates

Sized from the `/usage` counter on 2026-09-18 (window since 2026-09-15). Pickup trigger for both: the next session touching token cost.

- **`/status` response slimming** — `/status` is 72% of tracked response bytes (110 calls, 1.03 MB cumulative, ~9.4 KB per call), and the MCP wrapper doubles it (`JSON.stringify` into the text block plus structuredContent). A lean projection — URL, blocked/ready, the few fields callers read — cuts the dominant route ~10×. Same pass can slim `/runs/get`'s summary (~13 KB/call, second place, 18 calls).
- **Default MCP tool profile → core** — both registrations (user-scope and project in `~/.claude.json`) run bare `node mcp-server.mjs`, exposing 47 tool schemas to every session and again after every compaction; core is 11 and covers `/agentify`'s needs. One-line args change (`--tool-profile core`) plus a flip-back note for library tools. Likely the bigger per-session lever, zero code.
